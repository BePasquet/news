import styled from 'styled-components';
import { FlexColumnContainer } from './flex-column-container';

export const CenterContainer = styled(FlexColumnContainer)`
  height: 100%;
  justify-content: center;
  align-items: center;
`;
